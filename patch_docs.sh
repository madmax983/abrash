sed -i 's/    pub fn sphere(radius: f32, stacks: u32, sectors: u32) -> Self {/    \/\/\/ Optimization: Pre-allocating capacity avoids reallocations during mesh construction.\n    pub fn sphere(radius: f32, stacks: u32, sectors: u32) -> Self {/' crates/abrash-core/src/mesh.rs
sed -i 's/    pub fn plane(size: f32, subdivisions: u32) -> Self {/    \/\/\/ Optimization: Pre-allocating capacity avoids reallocations during mesh construction.\n    pub fn plane(size: f32, subdivisions: u32) -> Self {/' crates/abrash-core/src/mesh.rs
sed -i 's/    pub fn cylinder(radius: f32, height: f32, sectors: u32, stacks: u32) -> Self {/    \/\/\/ Optimization: Pre-allocating capacity avoids reallocations during mesh construction.\n    pub fn cylinder(radius: f32, height: f32, sectors: u32, stacks: u32) -> Self {/' crates/abrash-core/src/mesh.rs
sed -i 's/    pub fn torus(/    \/\/\/ Optimization: Pre-allocating capacity avoids reallocations during mesh construction.\n    pub fn torus(/' crates/abrash-core/src/mesh.rs
