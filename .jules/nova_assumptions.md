Assumption: I encountered a failing test (`havoc_line_3d_overflow`) when running `cargo test -p abrash-render --features nova`. This test is entirely unrelated to my new feature (`radar_demo`), and is failing because of a pre-existing integer overflow vulnerability in `crates/abrash-render/src/rasterizer/line.rs` which was exposed by the 'Havoc' testing persona. As per my instructions ("do not get derailed attempting to fix them. Revert any accidental modifications to those tests, document the pre-existing failures as an assumption in the PR, and proceed with your primary objective"), I am not fixing this bug, as it is outside the scope of my additive Nova objective.
