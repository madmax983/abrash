## [Reduction]
**Bloat:** Unused "experimental" modules (`ascii.rs`, `procedural.rs`, `svg_renderer.rs`, `terrain.rs`, `text.rs`, `vhs.rs`) and their tests.
**Cut:** Deleted 6 files and associated tests.
**Saved:** ~1000 lines of code / maintenance burden.

## [Reduction]
**Bloat:** Unused function `clip_triangle_against_near_plane` in `src/clipping.rs`.
**Cut:** Deleted function and tests.
**Saved:** ~150 lines of code.
