## CLI Error Output Tables
**Concept:** Replaced raw exit code / string dumps on crash with nicely formatted comfy-tables.
**Fate:** Completed
**Lesson:** `comfy_table` works great for replacing raw errors by creating structured "Issue" vs "Details" blocks instead of wall-of-text JSON/OS error messages.
