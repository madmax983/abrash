1. Add a test in `crates/abrash-core/src/framebuffer.rs` for `Framebuffer::new` with dimensions too large (e.g. `u32::MAX`).
2. Add a test in `crates/abrash-core/src/zbuffer.rs` for `ZBuffer::new` with dimensions too large (e.g. `u32::MAX`).
3. Complete pre commit steps.
