1. Refactor `collect()` into `extend()` in `gltf_loader.rs` to reuse vector capacities and eliminate intermediate dynamic allocations.
2. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the changes.
