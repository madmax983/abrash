1. Add missing documentation and examples to the `embed-demo` crate (`EmbedCamera`, `EmbedDraw`, `EmbedScene`). (Completed)
2. Add missing documentation and examples to the `abrash-anim` crate (`clock::PlaybackMode`, `clock::ClockEvent`, `clock::AnimationClock`). Fixed `AnimationClock` example tests. (Completed)
3. Add missing documentation and examples to the `abrash-skeletal` crate (`pose::Pose`, `skin::SkinData`). (Completed)
4. Verify tests pass for `embed-demo`, `abrash-anim`, and `abrash-skeletal`.
5. Pre-commit check - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
