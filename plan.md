1. Modify `blit_alpha_unchecked` and `blit_opaque_unchecked` in `crates/abrash-core/src/blitter.rs`.
   - Update `blit_alpha_unchecked` to use `iter_mut().zip(iter())` pattern for row copying to elide inner loop bounds checks.
   - Update `blit_opaque_unchecked` to use `dst_slice.copy_from_slice(src_slice)` to elide inner loop bounds checks.
   - Suppress any warnings generated by the unsafe get unchecked operations since they are guaranteed correct due to the parent caller clip rect.
2. Update `.jules/bolt.md` to record the performance learnings.
   - Describe the replacement of bounds checked indexing with iterator zipping and direct slice copies.
3. Test the changes using `cargo test -p abrash-core` and running `cargo bench --bench blitter` to verify optimizations.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
5. Create a pull request.
   - Title: `⚡ Bolt: Optimize inner loop bounds checking in unchecked blit operations`
   - Description sections: '💡 What', '🎯 Why', '📊 Impact', '🔬 Measurement'
