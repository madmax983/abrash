<<<<<<< SEARCH
            // Using + 0.5 cast to i32 is significantly faster than f32::round()
            // since we are iterating linearly. In Rust, we need to handle negative
            // numbers correctly, so we use `+ sign * 0.5`. Wait, f32 to i32 truncates.
            let cx_scaled = if rx_scaled >= 0.0 { (rx_scaled + 0.5) as i32 as f32 } else { (rx_scaled - 0.5) as i32 as f32 };
            let cy_scaled = if ry_scaled >= 0.0 { (ry_scaled + 0.5) as i32 as f32 } else { (ry_scaled - 0.5) as i32 as f32 };

            let cx = cx_scaled * dot_size;
            let cy = cy_scaled * dot_size;
=======
            // Using f32::round() is standard, but some archs are slow.
            // We can just use f32::round() or actually we can offset rx and ry before the loop
            // wait, round is fine if we use the right instr. Let's revert to round.
            let cx_scaled = rx_scaled.round();
            let cy_scaled = ry_scaled.round();

            let cx = cx_scaled * dot_size;
            let cy = cy_scaled * dot_size;
>>>>>>> REPLACE
