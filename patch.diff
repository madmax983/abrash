--- crates/abrash-render/src/rasterizer/circle.rs
+++ crates/abrash-render/src/rasterizer/circle.rs
@@ -109,26 +109,28 @@
     y: i32,
     color: u32,
 ) {
+    let w = fb.width() as usize;
+    let slice = fb.as_mut_slice();
     unsafe {
-        fb.set_pixel_unchecked((xc + x) as usize, (yc + y) as usize, color);
+        *slice.get_unchecked_mut(((yc + y) as usize) * w + ((xc + x) as usize)) = color;
         if x != 0 {
-            fb.set_pixel_unchecked((xc - x) as usize, (yc + y) as usize, color);
+            *slice.get_unchecked_mut(((yc + y) as usize) * w + ((xc - x) as usize)) = color;
         }
         if y != 0 {
-            fb.set_pixel_unchecked((xc + x) as usize, (yc - y) as usize, color);
+            *slice.get_unchecked_mut(((yc - y) as usize) * w + ((xc + x) as usize)) = color;
         }
         if x != 0 && y != 0 {
-            fb.set_pixel_unchecked((xc - x) as usize, (yc - y) as usize, color);
+            *slice.get_unchecked_mut(((yc - y) as usize) * w + ((xc - x) as usize)) = color;
         }
         if x != y {
-            fb.set_pixel_unchecked((xc + y) as usize, (yc + x) as usize, color);
+            *slice.get_unchecked_mut(((yc + x) as usize) * w + ((xc + y) as usize)) = color;
             if y != 0 {
-                fb.set_pixel_unchecked((xc - y) as usize, (yc + x) as usize, color);
+                *slice.get_unchecked_mut(((yc + x) as usize) * w + ((xc - y) as usize)) = color;
             }
             if x != 0 {
-                fb.set_pixel_unchecked((xc + y) as usize, (yc - x) as usize, color);
+                *slice.get_unchecked_mut(((yc - x) as usize) * w + ((xc + y) as usize)) = color;
             }
             if x != 0 && y != 0 {
-                fb.set_pixel_unchecked((xc - y) as usize, (yc - x) as usize, color);
+                *slice.get_unchecked_mut(((yc - x) as usize) * w + ((xc - y) as usize)) = color;
             }
         }
     }
}
