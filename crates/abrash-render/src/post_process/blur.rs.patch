--- src/post_process/blur.rs	2024-06-25 15:00:00.000000000 -0400
+++ src/post_process/blur.rs	2024-06-25 15:00:00.000000000 -0400
@@ -231,6 +231,7 @@
     width: usize,
     height: usize,
     radius: u32,
 ) {
+    let radius = radius.min((width.max(height)) as u32);
     let radius = radius as usize;
     // Window size (kernel width)
     let kernel_size = (2 * radius + 1) as u64;
